<main id="login">
    <h2>Connexion</h2>
    <form action="/projet-final-v2/connexion" method="post">
        <p class="form-errors">
            <?php
            if (isset($data["errors"]))
                echo $data["errors"][0];
            ?>
        </p>
        <input type="hidden" name="form-name" id="form-name" value="login" />
        <fieldset>
            <label for="login-email">
                Email
            </label>
            <input type="email" name="login-email" id="login-email" />
        </fieldset>
        <fieldset>
            <label for="login-password">
                Mot de passe
            </label>
            <input type="password" name="login-password" id="login-password" minlength="12" required />
        </fieldset>
        <fieldset>
            <button type="submit">
                Se connecter
            </button>
        </fieldset>
        <a href="/projet-final-v2/créer-un-compte">Pas encore de compte ? Créez-en un</a>
    </form>
</main>